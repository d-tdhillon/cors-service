{
  "name": "cors-service-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "func start",
    "test": "npm run build && node --test test/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "@azure/identity": "^4.7.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
